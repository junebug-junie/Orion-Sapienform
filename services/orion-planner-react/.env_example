# ─────────────────────────────────────────────
# 🤖 Orion ReAct Planner Service
# ─────────────────────────────────────────────

SERVICE_NAME=planner-react
SERVICE_VERSION=0.1.0
PLANNER_PORT=8090

# LLM + Backend
#ORION_LLM_BACKEND_URL=http://orion-ollama-host:11434

# Bus (optional for later extensions)
ORION_BUS_ENABLED=true
ORION_BUS_ENFORCE_CATALOG=true
ORION_BUS_URL=redis://100.92.216.81:6379/0

# Which bus service name the gateway listens on
LLM_GATEWAY_SERVICE_NAME=LLMGatewayService
# Where Cortex + Hub send exec_step/chat/generate requests
EXEC_REQUEST_PREFIX=orion-exec:request

# --- Cortex Orchestrator integration ---
CORTEX_REQUEST_CHANNEL=orion-cortex:request
CORTEX_RESULT_PREFIX=orion-cortex:result

# Requests into service
PLANNER_REQUEST_CHANNEL=orion-exec:request:PlannerReactService
PLANNER_RESULT_PREFIX=orion-exec:result:PlannerReactService

# Logging
LOG_LEVEL=INFO
