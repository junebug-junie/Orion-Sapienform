services:
  cortex-orchestrator:
    build:
      context: ../..
      dockerfile: services/orion-cortex-orch/Dockerfile
    container_name: ${PROJECT}-cortex-orch
    env_file:
      - .env
    ports:
      - "8072:8072"
    restart: unless-stopped

    environment:
      NODE_NAME: ${NODE_NAME}
      SERVICE_NAME: ${SERVICE_NAME}
      SERVICE_VERSION: ${SERVICE_VERSION}

      HEARTBEAT_INTERVAL_SEC: ${HEARTBEAT_INTERVAL_SEC}

      ORION_BUS_URL: ${ORION_BUS_URL}
      ORION_BUS_ENFORCE_CATALOG: ${ORION_BUS_ENFORCE_CATALOG}
      ORION_BUS_ENABLED: ${ORION_BUS_ENABLED}

      ORION_STATE_ENABLED: ${ORION_STATE_ENABLED:-true}
      STATE_REQUEST_CHANNEL: ${STATE_REQUEST_CHANNEL:-orion:state:request}
      STATE_RESULT_PREFIX: ${STATE_RESULT_PREFIX:-orion:state:reply}
      STATE_TIMEOUT_SEC: ${STATE_TIMEOUT_SEC:-2.0}
      STATE_SCOPE: ${STATE_SCOPE:-global}
      STATE_NODE: ${STATE_NODE:-}

      EXEC_REQUEST_PREFIX: ${EXEC_REQUEST_PREFIX:-orion:exec:request}
      EXEC_RESULT_PREFIX: ${EXEC_RESULT_PREFIX:-orion:exec:result}

      ORION_CORTEX_STEP_TIMEOUT_MS: ${ORION_CORTEX_STEP_TIMEOUT_MS:-8000}
      ORCH_REQUEST_CHANNEL: ${ORCH_REQUEST_CHANNEL:-orion:cortex:request}
      ORCH_RESULT_PREFIX: ${ORCH_RESULT_PREFIX:-orion:cortex:result}
      CHANNEL_EXEC_REQUEST: ${CHANNEL_EXEC_REQUEST:-orion:cortex:exec:request}
      CHANNEL_EXEC_REQUEST_CHAT: ${CHANNEL_EXEC_REQUEST_CHAT:-orion:cortex:exec:request:chat}
      CHANNEL_EXEC_REQUEST_SPARK: ${CHANNEL_EXEC_REQUEST_SPARK:-orion:cortex:exec:request:spark}
      CHANNEL_EXEC_REQUEST_BACKGROUND: ${CHANNEL_EXEC_REQUEST_BACKGROUND:-orion:cortex:exec:request:background}
      EXEC_LANE_ROUTING_ENABLED: ${EXEC_LANE_ROUTING_ENABLED:-false}
      CHANNEL_EQUILIBRIUM_METACOG_TRIGGER: ${CHANNEL_EQUILIBRIUM_METACOG_TRIGGER:-orion:equilibrium:metacog:trigger}
      AUTO_ROUTER_LLM_ENABLED: ${AUTO_ROUTER_LLM_ENABLED:-false}
      AUTO_ROUTER_LLM_REQUEST_CHANNEL: ${AUTO_ROUTER_LLM_REQUEST_CHANNEL:-orion:exec:request:LLMGatewayService}
      AUTO_ROUTER_LLM_REPLY_PREFIX: ${AUTO_ROUTER_LLM_REPLY_PREFIX:-orion:llm:reply}

      API_HOST: ${API_HOST:-0.0.0.0}
      API_PORT: ${API_PORT:-8072}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}

      PUBLISH_CORTEX_ORCH_GRAMMAR: ${PUBLISH_CORTEX_ORCH_GRAMMAR:-true}
      GRAMMAR_EVENT_CHANNEL: ${GRAMMAR_EVENT_CHANNEL:-orion:grammar:event}

      ORION_SUBSTRATE_TELEMETRY_BASE_URL: ${ORION_SUBSTRATE_TELEMETRY_BASE_URL:-}
      ORION_SUBSTRATE_TELEMETRY_TIMEOUT_SEC: ${ORION_SUBSTRATE_TELEMETRY_TIMEOUT_SEC:-2.0}
      ORION_SUBSTRATE_TELEMETRY_READ_TOKEN: ${ORION_SUBSTRATE_TELEMETRY_READ_TOKEN:-}

      CONCEPT_PROFILE_REPOSITORY_BACKEND: ${CONCEPT_PROFILE_REPOSITORY_BACKEND:-local}
      CONCEPT_PROFILE_BACKEND_CONCEPT_INDUCTION_PASS: ${CONCEPT_PROFILE_BACKEND_CONCEPT_INDUCTION_PASS:-}
      CONCEPT_PROFILE_GRAPH_CUTOVER_FALLBACK_POLICY: ${CONCEPT_PROFILE_GRAPH_CUTOVER_FALLBACK_POLICY:-fail_open_local}
      CONCEPT_PROFILE_GRAPHDB_ENDPOINT: ${CONCEPT_PROFILE_GRAPHDB_ENDPOINT:-}
      CONCEPT_PROFILE_GRAPHDB_URL: ${CONCEPT_PROFILE_GRAPHDB_URL:-}
      CONCEPT_PROFILE_GRAPHDB_REPO: ${CONCEPT_PROFILE_GRAPHDB_REPO:-collapse}
      CONCEPT_PROFILE_GRAPHDB_USER: ${CONCEPT_PROFILE_GRAPHDB_USER:-}
      CONCEPT_PROFILE_GRAPHDB_PASS: ${CONCEPT_PROFILE_GRAPHDB_PASS:-}
      CONCEPT_PROFILE_GRAPH_TIMEOUT_SEC: ${CONCEPT_PROFILE_GRAPH_TIMEOUT_SEC:-6}
      CONCEPT_PROFILE_GRAPH_URI: ${CONCEPT_PROFILE_GRAPH_URI:-http://conjourney.net/graph/spark/concept-profile}
      CONCEPT_PROFILE_PARITY_MIN_COMPARISONS: ${CONCEPT_PROFILE_PARITY_MIN_COMPARISONS:-50}
      CONCEPT_PROFILE_PARITY_MAX_MISMATCH_RATE: ${CONCEPT_PROFILE_PARITY_MAX_MISMATCH_RATE:-0.05}
      CONCEPT_PROFILE_PARITY_MAX_UNAVAILABLE_RATE: ${CONCEPT_PROFILE_PARITY_MAX_UNAVAILABLE_RATE:-0.02}
      CONCEPT_PROFILE_PARITY_CRITICAL_MISMATCH_CLASSES: ${CONCEPT_PROFILE_PARITY_CRITICAL_MISMATCH_CLASSES:-profile_missing_on_graph,profile_missing_on_local,query_error}
      CONCEPT_PROFILE_PARITY_SUMMARY_INTERVAL: ${CONCEPT_PROFILE_PARITY_SUMMARY_INTERVAL:-25}

      RECALL_PG_DSN: ${RECALL_PG_DSN:-}
      CHANNEL_RECALL_INTAKE: ${CHANNEL_RECALL_INTAKE:-orion:exec:request:RecallService}
      CHANNEL_RECALL_REPLY_PREFIX: ${CHANNEL_RECALL_REPLY_PREFIX:-orion:exec:result:RecallService}
      MIND_RECALL_PREFETCH_ENABLED: ${MIND_RECALL_PREFETCH_ENABLED:-true}
      MIND_RECALL_PREFETCH_TIMEOUT_SEC: ${MIND_RECALL_PREFETCH_TIMEOUT_SEC:-30}

      ORION_MIND_BASE_URL: ${ORION_MIND_BASE_URL:-}
      ORION_MIND_TIMEOUT_SEC: ${ORION_MIND_TIMEOUT_SEC:-210}
      MIND_N_LOOPS_DEFAULT: ${MIND_N_LOOPS_DEFAULT:-1}
      MIND_WALL_MS_DEFAULT: ${MIND_WALL_MS_DEFAULT:-180000}
      ORION_MIND_MAX_RESPONSE_BYTES: ${ORION_MIND_MAX_RESPONSE_BYTES:-2000000}
    networks:
      - app-net

networks:
  app-net:
    external: true
