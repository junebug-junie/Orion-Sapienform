# Supervisor Meta-Plan → finalize_response Evidence (Pass 3)

This evidence is produced by a deterministic harness (planner step + finalize action are stubbed; the meta-plan quality gate logic is real).

## Called tool ids
```
finalize_response
```

## Log excerpt
```
supervisor_wiring corr=corr-supervisor-meta-plan output_mode=implementation_guide profile=technical_delivery packs=['executive_pack', 'memory_pack'] tool_ids=['recall', 'extract_facts', 'goal_formulate', 'assess_risk', 'plan_action', 'tag_enrich', 'pattern_detect', 'story_weave', 'summarize_context', 'triage', 'analyze_text', 'evaluate']
supervisor_finalize_response_invoked=1 corr=corr-supervisor-meta-plan output_mode=implementation_guide (planner meta-plan)
```

## Final answer excerpt
```text
Discord Deployment Guide (Concrete)

This is the concrete output generated by `finalize_response`, not meta-planning scaffolding.

```