# Skeleton only (Patch 1, docs/superpowers/specs/2026-08-20-reverie-visual-
# chain-design.md). Deliberately NOT including diffusers/torch here -- that
# is real GPU weight, not a health-check skeleton dependency. Added in the
# later patch that wires real generation.

fastapi==0.115.0
uvicorn[standard]==0.30.6

pydantic==2.9.2
pydantic-settings==2.5.2
loguru==0.7.2
