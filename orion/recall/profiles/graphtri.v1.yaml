profile: graphtri.v1
enable_vector: false
vector_top_k: 0
rdf_top_k: 12
max_per_source: 5
max_total_items: 14
time_decay_half_life_hours: 72
render_budget_tokens: 280
cards_top_k: 4
enable_query_expansion: true
enable_sql_timeline: true
sql_since_minutes: 20160 #14 days
sql_top_k: 4
relevance:
  backend_weights:
    vector: 0.0
    sql_timeline: 0.9
    sql_chat: 0.6
    rdf_chat: 0.5
    # falkor_chat/falkor_graphtri mirror rdf_chat/rdf's weight -- same
    # content on a different backend when RECALL_FALKOR_IN_CHAT /
    # RECALL_FALKOR_GRAPHTRI_IN_CHAT swap the source in. Without these, a
    # per-key override merge (fusion.py::_backend_weights) leaves Falkor-
    # sourced content at DEFAULT_BACKEND_WEIGHTS's generic fallback instead
    # of this profile's tuned value -- found in code review, PR
    # feat/recall-falkor-graphtri-fragments.
    falkor_chat: 0.5
    rdf: 0.45
    falkor_graphtri: 0.45
    cards: 0.3
  score_weight: 0.7
  text_similarity_weight: 0.2
  recency_weight: 0.1
  enable_recency: true
  recency_half_life_hours: 72
