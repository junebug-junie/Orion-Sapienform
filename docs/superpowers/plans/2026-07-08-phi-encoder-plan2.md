# φ Encoder Plan 2 — MLP objective, prerequisites, producer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship Plan 2 deferred from PR #888 — seed-v2 cognitive features, grammar-truth liveness, MLP encoder inference, offline fit/promote pipeline, versioned telemetry artifacts, and `PhiIntrinsicRewardV1` producer on `orion:self:phi_reward`.

**Architecture:** Extend `orion-spark-introspector` to HTTP-poll substrate-runtime for grammar-truth + execution trajectory, assemble `InnerStateFeaturesV1` at `seed-v2`, run numpy MLP inference when `ORION_PHI_ENCODER_ENABLED=true`, and emit reward events. Training lives entirely in `scripts/fit_phi_encoder.py`; artifacts under `${TELEMETRY_ROOT}/models/phi/encoders/`. Step 3a consumer (trace recorder + drive feed) is a **separate plan**: `docs/superpowers/plans/2026-07-08-phi-intrinsic-reward-value-learning.md`.

**Tech Stack:** Python 3, Pydantic v2, pytest (+ `pytest-asyncio`), numpy, httpx, FastAPI (substrate endpoint), Redis bus.

**Spec:** `docs/superpowers/specs/2026-07-08-phi-encoder-plan2-design.md`

---

## Scope, split, and anti-stub rules (read first)

| Capability | Plan 2 — BUILD NOW (fully, no stubs) | Separate plan (Step 3a) |
|---|---|---|
| Execution trajectory HTTP endpoint | ✅ substrate-runtime | — |
| seed-v2 cognitive features | ✅ inner_state.py | — |
| Grammar-truth HTTP gate | ✅ substrate_reads.py + worker | — |
| MLP inference + manifest load | ✅ phi_encoder.py | — |
| Offline fit / promote / rollback | ✅ scripts/fit_phi_encoder.py | — |
| PhiIntrinsicRewardV1 producer | ✅ worker.py | — |
| Channel + registry | ✅ phi_reward + schemas | — |
| PhiValueRecorder + episode reducer | — | Step 3a plan |
| Drive feed (phi_value_metabolism) | — | Step 3a plan |

**Anti-laziness:** No `TODO`/`pass`/`NotImplementedError` in shipped Plan-2 paths. Encoder default-off is fine; when enabled with valid manifest, forward pass must be real numpy math.

**Repo rules:**
- Branch/worktree: `git worktree add ../Orion-Sapienform-phi-encoder-plan2 -b feat/phi-encoder-plan2`
- `ORION_BUS_URL=redis://100.92.216.81:6379/0`
- Contract changes: `orion/schemas/registry.py` + `orion/bus/channels.yaml` same changeset
- After `.env_example` edits: `python scripts/sync_local_env_from_example.py`

---

## File structure (created / modified)

**Created:**
- `orion/schemas/telemetry/phi_encoder.py`
- `services/orion-spark-introspector/app/substrate_reads.py`
- `services/orion-spark-introspector/app/phi_encoder.py`
- `scripts/fit_phi_encoder.py`
- `fixtures/phi_encoder_promote_gate.jsonl` (generated by fit script test helper)
- `tests/test_phi_encoder_schema.py`
- `tests/test_phi_encoder_mlp.py`
- `tests/test_phi_encoder_fit_script.py`
- `tests/test_inner_state_trajectory_features.py`
- `services/orion-spark-introspector/tests/test_substrate_reads.py`
- `services/orion-spark-introspector/tests/test_phi_reward_emit.py`
- `services/orion-substrate-runtime/tests/test_execution_trajectory_endpoint.py`
- `services/orion-spark-introspector/evals/eval_phi_encoder_fit.py`
- `services/orion-spark-introspector/evals/eval_phi_reward_bounded.py`

**Modified:**
- `services/orion-substrate-runtime/app/main.py`
- `services/orion-spark-introspector/app/inner_state.py`
- `services/orion-spark-introspector/app/worker.py`
- `services/orion-spark-introspector/app/settings.py` + `.env_example`
- `orion/schemas/registry.py`
- `orion/bus/channels.yaml`
- `services/orion-sql-writer/` (phi_reward durable subscribe — minimal)
- `services/orion-hub/` (inner_features consumer if missing — grep first)

---

## Task 1: Substrate `GET /projections/execution_trajectory`

**Files:**
- Create: `services/orion-substrate-runtime/tests/test_execution_trajectory_endpoint.py`
- Modify: `services/orion-substrate-runtime/app/main.py`

- [ ] **Step 1: Write the failing test**

```python
from __future__ import annotations

from datetime import datetime, timezone
from unittest.mock import MagicMock

import pytest
from httpx import ASGITransport, AsyncClient

from orion.schemas.execution_projection import ExecutionRunStateV1, ExecutionTrajectoryProjectionV1
from orion.substrate.execution_loop.constants import EXECUTION_TRAJECTORY_PROJECTION_ID


@pytest.mark.asyncio
async def test_execution_trajectory_endpoint_returns_projection(monkeypatch) -> None:
    import app.main as main

    now = datetime(2026, 7, 8, 12, 0, tzinfo=timezone.utc)
    proj = ExecutionTrajectoryProjectionV1(
        projection_id=EXECUTION_TRAJECTORY_PROJECTION_ID,
        generated_at=now,
        runs={
            "trace-1": ExecutionRunStateV1(
                trace_id="trace-1",
                correlation_id="corr-1",
                node_id="athena",
                reasoning_present=True,
                recall_observed=True,
                step_count=4,
                failed_step_count=1,
                pressure_hints={"execution_friction": 0.2},
                last_updated_at=now,
            )
        },
    )
    store = MagicMock()
    store.load_execution_trajectory.return_value = proj
    monkeypatch.setattr(main.worker, "_store", store, raising=False)

    transport = ASGITransport(app=main.app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.get("/projections/execution_trajectory")

    assert resp.status_code == 200
    body = resp.json()
    assert body["ok"] is True
    assert body["projection"]["runs"]["trace-1"]["reasoning_present"] is True
    store.load_execution_trajectory.assert_called_once_with(EXECUTION_TRAJECTORY_PROJECTION_ID)


@pytest.mark.asyncio
async def test_execution_trajectory_endpoint_no_projection(monkeypatch) -> None:
    import app.main as main

    store = MagicMock()
    store.load_execution_trajectory.return_value = None
    monkeypatch.setattr(main.worker, "_store", store, raising=False)

    transport = ASGITransport(app=main.app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.get("/projections/execution_trajectory")

    assert resp.status_code == 200
    assert resp.json() == {"ok": False, "reason": "no_projection"}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest services/orion-substrate-runtime/tests/test_execution_trajectory_endpoint.py -v`
Expected: FAIL — 404 (route missing)

- [ ] **Step 3: Add endpoint to main.py**

At top of `services/orion-substrate-runtime/app/main.py`, add import:

```python
from orion.substrate.execution_loop.constants import EXECUTION_TRAJECTORY_PROJECTION_ID
```

After `grammar_truth` route:

```python
@app.get("/projections/execution_trajectory")
async def execution_trajectory() -> dict:
    proj = worker._store.load_execution_trajectory(EXECUTION_TRAJECTORY_PROJECTION_ID)
    if proj is None:
        return {"ok": False, "reason": "no_projection"}
    return {"ok": True, "projection": proj.model_dump(mode="json")}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest services/orion-substrate-runtime/tests/test_execution_trajectory_endpoint.py -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add services/orion-substrate-runtime/app/main.py services/orion-substrate-runtime/tests/test_execution_trajectory_endpoint.py
git commit -m "feat(substrate-runtime): expose execution trajectory projection HTTP read"
```

---

## Task 2: Phi encoder schemas + registry + channel

**Files:**
- Create: `orion/schemas/telemetry/phi_encoder.py`
- Create: `tests/test_phi_encoder_schema.py`
- Modify: `orion/schemas/registry.py`
- Modify: `orion/bus/channels.yaml`

- [ ] **Step 1: Write schema module**

Create `orion/schemas/telemetry/phi_encoder.py`:

```python
"""Phi encoder manifest + intrinsic reward schemas (Plan 2)."""
from __future__ import annotations

from datetime import datetime
from typing import Dict, List, Literal, Optional

from pydantic import BaseModel, ConfigDict, Field


class CorpusStatsV1(BaseModel):
    model_config = ConfigDict(extra="forbid")

    corpus_path: str
    row_count: int
    excluded_degenerate: int
    time_range_start: Optional[datetime] = None
    time_range_end: Optional[datetime] = None


class TrainingStatsV1(BaseModel):
    model_config = ConfigDict(extra="forbid")

    epochs: int
    final_loss: float
    held_out_loss: float
    recon_error_p50: float
    recon_error_p95: float


class PhiEncoderManifestV1(BaseModel):
    model_config = ConfigDict(extra="forbid")

    encoder_id: str
    encoder_version: str
    parent_version: Optional[str] = None
    status: Literal["candidate", "active", "retired"]
    architecture: str
    features_version: str
    input_features: List[str]
    hidden_dim: int
    latent_dim: int
    corpus: CorpusStatsV1
    training: TrainingStatsV1
    probes: Dict[str, Dict[str, float]] = Field(default_factory=dict)
    git_sha: str
    trained_at: datetime
    promoted_at: Optional[datetime] = None


class AttributionV1(BaseModel):
    model_config = ConfigDict(extra="forbid")

    feature: str
    raw_value: float
    scaled_value: float
    attribution: float


class PhiIntrinsicRewardV1(BaseModel):
    model_config = ConfigDict(extra="forbid")

    generated_at: datetime
    self_state_id: Optional[str] = None
    encoder_version: str
    features_version: str
    phi: float
    delta_phi: float
    recon_error: float
    delta_recon_error: float
    latent: Dict[str, float] = Field(default_factory=dict)
    tick_metadata_window_sec: int = 2
    phi_health: str = "ok"
    grammar_truth_degraded: bool = False
    attribution_top: List[AttributionV1] = Field(default_factory=list)
```

- [ ] **Step 2: Write failing registry test**

Create `tests/test_phi_encoder_schema.py`:

```python
from orion.schemas.registry import SCHEMA_REGISTRY
from orion.schemas.telemetry.phi_encoder import PhiEncoderManifestV1, PhiIntrinsicRewardV1


def test_registry_resolves_phi_encoder_schemas() -> None:
    assert SCHEMA_REGISTRY["PhiEncoderManifestV1"] is PhiEncoderManifestV1
    assert SCHEMA_REGISTRY["PhiIntrinsicRewardV1"] is PhiIntrinsicRewardV1
```

- [ ] **Step 3: Run test — expect FAIL**

Run: `pytest tests/test_phi_encoder_schema.py -v`

- [ ] **Step 4: Register schemas + channel**

In `orion/schemas/registry.py`: import new models; add to `_REGISTRY` and `SCHEMA_REGISTRY`.

In `orion/bus/channels.yaml` after `orion:self:inner_features`:

```yaml
  - name: "orion:self:phi_reward"
    kind: "telemetry"
    schema_id: "PhiIntrinsicRewardV1"
    message_kind: "self.phi_reward.v1"
    producer_services: ["orion-spark-introspector"]
    consumer_services: ["orion-substrate-runtime", "orion-sql-writer", "orion-hub"]
    stability: "experimental"
    since: "2026-07-08"
```

- [ ] **Step 5: Run gates**

```bash
pytest tests/test_phi_encoder_schema.py -v
python scripts/check_schema_registry.py
python scripts/check_bus_channels.py
```

- [ ] **Step 6: Commit**

```bash
git add orion/schemas/telemetry/phi_encoder.py orion/schemas/registry.py orion/bus/channels.yaml tests/test_phi_encoder_schema.py
git commit -m "feat(contracts): PhiEncoderManifestV1 + PhiIntrinsicRewardV1 + phi_reward channel"
```

---

## Task 3: HTTP substrate reads (grammar-truth + trajectory)

**Files:**
- Create: `services/orion-spark-introspector/app/substrate_reads.py`
- Create: `services/orion-spark-introspector/tests/test_substrate_reads.py`

- [ ] **Step 1: Write failing tests**

```python
from __future__ import annotations

import time
from unittest.mock import AsyncMock

import pytest

from app.substrate_reads import SubstrateReadCache, fetch_grammar_truth, fetch_execution_trajectory


@pytest.mark.asyncio
async def test_fetch_grammar_truth_degraded(monkeypatch) -> None:
  client = AsyncMock()
  client.get.return_value.json.return_value = {"ok": False, "degraded": True, "degraded_reasons": ["x"]}
  client.get.return_value.raise_for_status = lambda: None
  out = await fetch_grammar_truth(client, "http://substrate/grammar/truth")
  assert out.degraded is True
  assert out.degraded_reasons == ["x"]


@pytest.mark.asyncio
async def test_fetch_grammar_truth_timeout_fail_closed(monkeypatch) -> None:
  client = AsyncMock()
  client.get.side_effect = TimeoutError("slow")
  out = await fetch_grammar_truth(client, "http://substrate/grammar/truth")
  assert out.degraded is True
  assert "http_error" in out.degraded_reasons[0]


def test_cache_reuses_within_ttl() -> None:
  cache = SubstrateReadCache(ttl_sec=2.0)
  cache.put_grammar({"degraded": False})
  assert cache.get_grammar() == {"degraded": False}
```

- [ ] **Step 2: Run tests — expect FAIL**

Run: `pytest services/orion-spark-introspector/tests/test_substrate_reads.py -v`

- [ ] **Step 3: Implement substrate_reads.py**

Key types:

```python
@dataclass(frozen=True)
class GrammarTruthSnapshot:
    degraded: bool
    degraded_reasons: list[str]
    enabled_reducers: dict[str, bool]
    reducer_health_by_name: dict[str, dict]

@dataclass(frozen=True)
class ExecutionTrajectorySnapshot:
    ok: bool
    projection: dict | None
```

Implement:
- `SubstrateReadCache` with monotonic TTL for grammar + trajectory
- `fetch_grammar_truth(client, url) -> GrammarTruthSnapshot` — on any error return `degraded=True`, `degraded_reasons=["http_error:..."]`
- `fetch_execution_trajectory(client, url) -> ExecutionTrajectorySnapshot`
- `cognitive_lane_dark(snapshot: GrammarTruthSnapshot) -> bool` — checks `enabled_reducers.execution_trajectory` and health classification in `{dead_no_heartbeat, blocked_on_event, cursor_commit_failing}`

- [ ] **Step 4: Run tests — expect PASS**

- [ ] **Step 5: Commit**

---

## Task 4: seed-v2 cognitive trajectory features

**Files:**
- Create: `tests/test_inner_state_trajectory_features.py`
- Modify: `services/orion-spark-introspector/app/inner_state.py`

- [ ] **Step 1: Write failing tests**

```python
from datetime import datetime, timedelta, timezone

import importlib.util
from pathlib import Path

_SPEC = importlib.util.spec_from_file_location(
    "spark_inner_state",
    Path(__file__).resolve().parents[1] / "services" / "orion-spark-introspector" / "app" / "inner_state.py",
)
inner_state = importlib.util.module_from_spec(_SPEC)
_SPEC.loader.exec_module(inner_state)


def test_execution_trajectory_features_reasoning_present() -> None:
    now = datetime(2026, 7, 8, 12, 0, tzinfo=timezone.utc)
    projection = {
        "runs": {
            "a": {
                "reasoning_present": True,
                "recall_observed": True,
                "step_count": 10,
                "failed_step_count": 2,
                "pressure_hints": {"execution_friction": 0.4},
                "last_updated_at": now.isoformat(),
            }
        }
    }
    feats = inner_state.cognitive_features_from_trajectory(
        projection, now=now, max_age_sec=120,
    )
    by_name = {f.name: f for f in feats}
    assert by_name["reasoning_present"].raw_value == 1.0
    assert by_name["recall_gate_fired"].raw_value == 1.0
    assert abs(by_name["exec_step_fail_rate"].raw_value - 0.2) < 1e-6


def test_execution_trajectory_stale_runs_excluded() -> None:
    now = datetime(2026, 7, 8, 12, 0, tzinfo=timezone.utc)
    stale = now - timedelta(seconds=300)
    projection = {
        "runs": {
            "a": {
                "reasoning_present": True,
                "recall_observed": True,
                "step_count": 1,
                "failed_step_count": 0,
                "pressure_hints": {},
                "last_updated_at": stale.isoformat(),
            }
        }
    }
    feats = inner_state.cognitive_features_from_trajectory(
        projection, now=now, max_age_sec=120,
    )
    assert all(f.source == "execution_trajectory.none" for f in feats)
    assert all(f.raw_value == 0.0 for f in feats)
```

- [ ] **Step 2: Run — expect FAIL**

- [ ] **Step 3: Implement in inner_state.py**

Add:

```python
COGNITIVE_FEATURE_NAMES: Tuple[str, ...] = (
    "recall_gate_fired",
    "reasoning_present",
    "exec_step_fail_rate",
    "execution_friction",
)

def cognitive_features_from_trajectory(
    projection: dict | None,
    *,
    now: datetime,
    max_age_sec: int,
) -> List[InnerFeatureV1]:
    """Return four cognitive InnerFeatureV1 rows (raw only; scaling in build_inner_state_features)."""
    none_source = "execution_trajectory.none"
    if not projection or not projection.get("runs"):
        return [
            InnerFeatureV1(name=n, raw_value=0.0, scaled_value=0.0, source=none_source)
            for n in COGNITIVE_FEATURE_NAMES
        ]
    active = []
    for run in projection["runs"].values():
        ts = datetime.fromisoformat(str(run["last_updated_at"]).replace("Z", "+00:00"))
        if (now - ts).total_seconds() <= max_age_sec:
            active.append(run)
    if not active:
        return [
            InnerFeatureV1(name=n, raw_value=0.0, scaled_value=0.0, source=none_source)
            for n in COGNITIVE_FEATURE_NAMES
        ]
    recall = 1.0 if any(r.get("recall_observed") for r in active) else 0.0
    reasoning_frac = sum(1 for r in active if r.get("reasoning_present")) / len(active)
    steps = sum(int(r.get("step_count") or 0) for r in active)
    fails = sum(int(r.get("failed_step_count") or 0) for r in active)
    fail_rate = fails / max(1, steps)
    frictions = [float(r.get("pressure_hints", {}).get("execution_friction", 0.0)) for r in active]
    friction = sum(frictions) / len(frictions)
    raw_by_name = {
        "recall_gate_fired": recall,
        "reasoning_present": reasoning_frac,
        "exec_step_fail_rate": fail_rate,
        "execution_friction": friction,
    }
    return [
        InnerFeatureV1(
            name=name,
            raw_value=round(raw_by_name[name], 4),
            scaled_value=0.0,
            source=f"execution_trajectory.runs.*.{name}",
        )
        for name in COGNITIVE_FEATURE_NAMES
    ]
```

Extend `build_inner_state_features(...)` signature:

```python
def build_inner_state_features(
    ss,
    scaler: RollingRobustScaler,
    *,
    features_version: str,
    grammar_degraded: bool,
    degraded_reasons: Optional[List[str]] = None,
    trajectory_projection: Optional[dict] = None,
    exec_trajectory_max_age_sec: int = 120,
    prev_felt: Optional[Tuple[float, ...]] = None,
    prev_headline: Optional[float] = None,
    degenerate_streak: int = 0,
    degenerate_limit: int = 20,
) -> Tuple[InnerStateFeaturesV1, Tuple[float, ...], int]:
```

After felt features loop, append cognitive features (observe_and_scale each). Put `degraded_reasons` into `metadata` when present.

- [ ] **Step 4: Run tests**

```bash
pytest tests/test_inner_state_trajectory_features.py tests/test_inner_state_features.py -v
```

- [ ] **Step 5: Commit**

---

## Task 5: Settings, env, corpus path migration

**Files:**
- Modify: `services/orion-spark-introspector/app/settings.py`
- Modify: `services/orion-spark-introspector/.env_example`

- [ ] **Step 1: Add settings fields**

```python
inner_features_version: str = Field("seed-v2", alias="INNER_FEATURES_VERSION")
substrate_runtime_url: str = Field(
    "http://orion-athena-substrate-runtime:8115",
    alias="SUBSTRATE_RUNTIME_URL",
)
substrate_read_timeout_sec: float = Field(2.0, alias="SUBSTRATE_READ_TIMEOUT_SEC")
substrate_read_cache_sec: float = Field(2.0, alias="SUBSTRATE_READ_CACHE_SEC")
exec_trajectory_max_age_sec: int = Field(120, alias="EXEC_TRAJECTORY_MAX_AGE_SEC")
orion_phi_encoder_hidden_dim: int = Field(16, alias="PHI_ENCODER_HIDDEN_DIM")
orion_phi_encoder_latent_dim: int = Field(8, alias="PHI_ENCODER_LATENT_DIM")
channel_phi_reward: str = Field("orion:self:phi_reward", alias="CHANNEL_PHI_REWARD")
```

Update `.env_example`:

```text
INNER_FEATURES_VERSION=seed-v2
INNER_FEATURES_CORPUS_PATH=/mnt/telemetry/phi/corpus/inner_state.jsonl
SUBSTRATE_RUNTIME_URL=http://orion-athena-substrate-runtime:8115
SUBSTRATE_READ_TIMEOUT_SEC=2.0
SUBSTRATE_READ_CACHE_SEC=2.0
EXEC_TRAJECTORY_MAX_AGE_SEC=120
ORION_PHI_ENCODER_WEIGHTS=/mnt/telemetry/models/phi/encoders/active
PHI_ENCODER_HIDDEN_DIM=16
PHI_ENCODER_LATENT_DIM=8
CHANNEL_PHI_REWARD=orion:self:phi_reward
```

- [ ] **Step 2: Sync local env**

```bash
python scripts/sync_local_env_from_example.py
python scripts/check_env_template_parity.py
```

- [ ] **Step 3: Commit**

---

## Task 6: Wire grammar-truth + trajectory into handle_self_state

**Files:**
- Modify: `services/orion-spark-introspector/app/worker.py`
- Modify: `services/orion-spark-introspector/tests/test_inner_state_emit.py`

- [ ] **Step 1: Add failing tests**

Extend `test_inner_state_emit.py`:

```python
from dataclasses import dataclass
from unittest.mock import AsyncMock

import app.worker as worker
from app.substrate_reads import ExecutionTrajectorySnapshot, GrammarTruthSnapshot


@dataclass
class _Gt:
    degraded: bool
    degraded_reasons: list
    enabled_reducers: dict
    reducer_health_by_name: dict


@pytest.mark.asyncio
async def test_handle_self_state_grammar_truth_freeze(monkeypatch) -> None:
    monkeypatch.setattr(
        worker,
        "fetch_grammar_truth",
        AsyncMock(return_value=GrammarTruthSnapshot(
            degraded=True,
            degraded_reasons=["cursor_lag:execution_grammar_reducer"],
            enabled_reducers={"execution_trajectory": True},
            reducer_health_by_name={},
        )),
    )
    monkeypatch.setattr(
        worker,
        "fetch_execution_trajectory",
        AsyncMock(return_value=ExecutionTrajectorySnapshot(ok=False, projection=None)),
    )
    captured = {}

    class _Bus:
        enabled = True
        async def publish(self, channel, env):
            captured["payload"] = env.payload

    monkeypatch.setattr(worker, "_pub_bus", _Bus(), raising=False)
    # reuse _self_state_payload() + BaseEnvelope from existing emit tests
    await worker.handle_self_state(_envelope())
    assert captured["payload"].phi_health == "frozen"


@pytest.mark.asyncio
async def test_handle_self_state_includes_cognitive_features(monkeypatch) -> None:
    now = _NOW
    monkeypatch.setattr(
        worker,
        "fetch_grammar_truth",
        AsyncMock(return_value=GrammarTruthSnapshot(
            degraded=False,
            degraded_reasons=[],
            enabled_reducers={"execution_trajectory": True},
            reducer_health_by_name={"execution_trajectory": {"classification": "healthy"}},
        )),
    )
    monkeypatch.setattr(
        worker,
        "fetch_execution_trajectory",
        AsyncMock(return_value=ExecutionTrajectorySnapshot(
            ok=True,
            projection={"runs": {"a": {
                "reasoning_present": True,
                "recall_observed": True,
                "step_count": 4,
                "failed_step_count": 0,
                "pressure_hints": {},
                "last_updated_at": now.isoformat(),
            }}},
        )),
    )
    captured = {}

    class _Bus:
        enabled = True
        async def publish(self, channel, env):
            captured["payload"] = env.payload

    monkeypatch.setattr(worker, "_pub_bus", _Bus(), raising=False)
    await worker.handle_self_state(_envelope())
    names = {f.name for f in captured["payload"].features}
    assert "reasoning_present" in names
```

- [ ] **Step 2: Run — expect FAIL**

- [ ] **Step 3: Wire worker**

In `handle_self_state`, before `build_inner_state_features`:

```python
from .substrate_reads import (
    SubstrateReadCache,
    cognitive_lane_dark,
    fetch_execution_trajectory,
    fetch_grammar_truth,
)

# module-level: _SUBSTRATE_CACHE = SubstrateReadCache(ttl_sec=settings.substrate_read_cache_sec)
# reuse _cola_http_client pattern or dedicated httpx.AsyncClient

gt = await fetch_grammar_truth(_substrate_client(), f"{settings.substrate_runtime_url}/grammar/truth")
traj = await fetch_execution_trajectory(
    _substrate_client(),
    f"{settings.substrate_runtime_url}/projections/execution_trajectory",
)
grammar_degraded = gt.degraded or cognitive_lane_dark(gt)
projection = traj.projection if traj.ok else None

inner, _INNER_PREV_FELT, _INNER_DEGENERATE_STREAK = build_inner_state_features(
    ss,
    _INNER_SCALER,
    features_version=settings.inner_features_version,
    grammar_degraded=grammar_degraded,
    degraded_reasons=gt.degraded_reasons,
    trajectory_projection=projection,
    exec_trajectory_max_age_sec=int(settings.exec_trajectory_max_age_sec),
    prev_felt=_INNER_PREV_FELT,
    prev_headline=_INNER_PREV_HEADLINE,
    degenerate_streak=_INNER_DEGENERATE_STREAK,
    degenerate_limit=int(settings.phi_degenerate_streak),
)
```

Remove `# Plan 2: wire...` comment and literal `False`.

- [ ] **Step 4: Run emit tests — expect PASS**

- [ ] **Step 5: Commit**

---

## Task 7: MLP encoder inference module

**Files:**
- Create: `services/orion-spark-introspector/app/phi_encoder.py`
- Create: `tests/test_phi_encoder_mlp.py`

- [ ] **Step 1: Write failing forward test**

```python
import json
import tempfile
from pathlib import Path

import numpy as np
import pytest

from orion.schemas.telemetry.phi_encoder import PhiEncoderManifestV1, CorpusStatsV1, TrainingStatsV1
from services.orion-spark-introspector.app.phi_encoder import PhiEncoderRuntime, PhiForwardResult


def _tiny_manifest(input_features: list[str]) -> PhiEncoderManifestV1:
    return PhiEncoderManifestV1(
        encoder_id="test",
        encoder_version="v0",
        status="candidate",
        architecture="mlp_shallow_v1",
        features_version="seed-v2",
        input_features=input_features,
        hidden_dim=4,
        latent_dim=2,
        corpus=CorpusStatsV1(corpus_path="x", row_count=1, excluded_degenerate=0),
        training=TrainingStatsV1(epochs=1, final_loss=0.1, held_out_loss=0.1, recon_error_p50=0.1, recon_error_p95=0.2),
        git_sha="test",
        trained_at=__import__("datetime").datetime.now(__import__("datetime").timezone.utc),
    )


def test_mlp_forward_phi_bounded(tmp_path: Path) -> None:
    feats = ["coherence", "overall_intensity"]
    manifest = _tiny_manifest(feats)
    d_in, h, d_lat = 2, 4, 2
    np.savez(
        tmp_path / "weights.npz",
        W1=np.random.randn(d_in, h).astype(np.float64) * 0.01,
        b1=np.zeros(h),
        W2=np.random.randn(h, d_lat).astype(np.float64) * 0.01,
        b2=np.zeros(d_lat),
        W3=np.random.randn(d_lat, d_in).astype(np.float64) * 0.01,
        b3=np.zeros(d_in),
        w_phi=np.array([0.1, 0.2]),
        b_phi=np.array(0.0),
    )
    (tmp_path / "manifest.json").write_text(manifest.model_dump_json())
    rt = PhiEncoderRuntime.load(tmp_path, expected_features_version="seed-v2")
    x = np.array([0.1, -0.2])
    out = rt.forward(x)
    assert isinstance(out, PhiForwardResult)
    assert 0.0 <= out.phi <= 1.0
    assert out.recon_error >= 0.0
    assert len(out.latent) == 2
```

- [ ] **Step 2: Run — expect FAIL**

- [ ] **Step 3: Implement phi_encoder.py**

```python
@dataclass(frozen=True)
class PhiForwardResult:
    phi: float
    recon_error: float
    latent: dict[str, float]
    attribution_top: list[AttributionV1]

class PhiEncoderRuntime:
    @classmethod
    def load(cls, weights_dir: Path, *, expected_features_version: str) -> "PhiEncoderRuntime | None":
        manifest_path = weights_dir / "manifest.json"
        weights_path = weights_dir / "weights.npz"
        if not manifest_path.is_file() or not weights_path.is_file():
            return None
        manifest = PhiEncoderManifestV1.model_validate_json(manifest_path.read_text())
        if manifest.features_version != expected_features_version:
            return None
        arrays = np.load(weights_path)
        return cls(manifest=manifest, arrays=arrays)

    def forward(self, x: np.ndarray) -> PhiForwardResult:
        h = np.maximum(0.0, x @ self._W1 + self._b1)
        z = h @ self._W2 + self._b2
        xhat = z @ self._W3 + self._b3
        phi = float(1.0 / (1.0 + np.exp(-(z @ self._w_phi + float(self._b_phi)))))
        recon = float(np.mean((x - xhat) ** 2))
        latent = {f"z{i}": float(z[i]) for i in range(z.shape[0])}
        attribution_top = self._top_attributions(x, z, phi)
        return PhiForwardResult(phi=phi, recon_error=recon, latent=latent, attribution_top=attribution_top)
```

- [ ] **Step 4: Run — expect PASS**

- [ ] **Step 5: Commit**

---

## Task 8: Offline fit script + corpus gates

**Files:**
- Create: `scripts/fit_phi_encoder.py`
- Create: `tests/test_phi_encoder_fit_script.py`
- Create: `fixtures/phi_encoder_promote_gate.jsonl` (small hand-authored or test-generated)

- [ ] **Step 1: Write failing corpus gate test**

```python
import subprocess
import sys
from pathlib import Path

REPO = Path(__file__).resolve().parents[1]


def test_fit_refuses_small_corpus(tmp_path: Path) -> None:
    corpus = tmp_path / "tiny.jsonl"
    corpus.write_text("")  # empty
    proc = subprocess.run(
        [sys.executable, str(REPO / "scripts" / "fit_phi_encoder.py"), "--corpus", str(corpus), "--out", str(tmp_path / "out")],
        capture_output=True,
        text=True,
    )
    assert proc.returncode != 0
    assert "min_rows" in proc.stdout.lower() or "min_rows" in proc.stderr.lower()
```

- [ ] **Step 2: Run — expect FAIL**

- [ ] **Step 3: Implement fit_phi_encoder.py**

CLI modes:
- default: train → write `manifest.json`, `weights.npz`, `probes.json` to `--out`
- `--eval-only --manifest <dir>`: run promote gates on fixture
- `--promote --version <ver> --encoders-root <path>`: symlink `active` + flip manifest status

Training:
- Load JSONL `InnerStateFeaturesV1` rows; skip `phi_health != ok` or `grammar_truth_degraded`
- Build matrix from `input_features` scaled values (ordered list in manifest)
- Corpus gates: min rows, min hours, min feature variance on 80% features
- SGD ~200 epochs, early stop; write probes (Pearson r per latent axis)

Promote gate: load `fixtures/phi_encoder_promote_gate.jsonl`, compute p95 recon ratio ≥ 2.0

- [ ] **Step 4: Run fit tests**

- [ ] **Step 5: Commit**

---

## Task 9: Encoder inference + PhiIntrinsicRewardV1 emit in worker

**Files:**
- Modify: `services/orion-spark-introspector/app/worker.py`
- Create: `services/orion-spark-introspector/tests/test_phi_reward_emit.py`

- [ ] **Step 1: Write failing reward emit test**

```python
@pytest.mark.asyncio
async def test_phi_reward_emitted_when_encoder_ok(monkeypatch, tmp_path) -> None:
    monkeypatch.setattr(worker.settings, "orion_phi_encoder_enabled", True, raising=False)
    monkeypatch.setattr(worker.settings, "orion_phi_encoder_weights", str(tmp_path), raising=False)
    # write tiny manifest + weights.npz using helper from test_phi_encoder_mlp
    _write_tiny_encoder(tmp_path)
    monkeypatch.setattr(worker, "_load_phi_encoder", worker.PhiEncoderRuntime.load, raising=False)
    published = []

    class _Bus:
        enabled = True
        async def publish(self, channel, env):
            published.append((channel, env))

    monkeypatch.setattr(worker, "_pub_bus", _Bus(), raising=False)
    monkeypatch.setattr(worker, "fetch_grammar_truth", AsyncMock(return_value=_grammar_ok()))
    monkeypatch.setattr(worker, "fetch_execution_trajectory", AsyncMock(return_value=_traj_empty()))
    await worker.handle_self_state(_envelope())
    reward_channels = [c for c, _ in published if c == worker.settings.channel_phi_reward]
    assert reward_channels, "expected phi_reward publish"


@pytest.mark.asyncio
async def test_phi_reward_suppressed_when_frozen(monkeypatch) -> None:
    monkeypatch.setattr(worker.settings, "orion_phi_encoder_enabled", True, raising=False)
    published = []

    class _Bus:
        enabled = True
        async def publish(self, channel, env):
            published.append(channel)

    monkeypatch.setattr(worker, "_pub_bus", _Bus(), raising=False)
    monkeypatch.setattr(worker, "fetch_grammar_truth", AsyncMock(return_value=_grammar_degraded()))
    await worker.handle_self_state(_envelope())
    assert worker.settings.channel_phi_reward not in published
```

- [ ] **Step 2: Run — expect FAIL**

- [ ] **Step 3: Wire worker**

Add module-level:

```python
_PHI_ENCODER: PhiEncoderRuntime | None = None
_PHI_PREV_PHI: float | None = None
_PHI_PREV_RECON: float | None = None

class PhiIntrinsicRewardEnvelope(Envelope[PhiIntrinsicRewardV1]):
    kind: str = "self.phi_reward.v1"
```

After building `inner`, if `settings.orion_phi_encoder_enabled` and `inner.phi_health == "ok"` and not `inner.grammar_truth_degraded`:

```python
enc = _load_phi_encoder()
if enc is not None:
    x = enc.feature_vector_from_inner(inner)
    out = enc.forward(x)
    inner.headline = round(out.phi, 4)
    inner.headline_source = "encoder"
    inner.metadata.update({
        "recon_error": out.recon_error,
        "latent": out.latent,
        "encoder_version": enc.encoder_version,
    })
    delta_phi = 0.0 if _PHI_PREV_PHI is None else out.phi - _PHI_PREV_PHI
    reward = PhiIntrinsicRewardV1(
        generated_at=inner.generated_at,
        self_state_id=inner.self_state_id,
        encoder_version=enc.encoder_version,
        features_version=inner.features_version,
        phi=out.phi,
        delta_phi=delta_phi,
        recon_error=out.recon_error,
        delta_recon_error=0.0 if _PHI_PREV_RECON is None else out.recon_error - _PHI_PREV_RECON,
        latent=out.latent,
        attribution_top=out.attribution_top,
        grammar_truth_degraded=inner.grammar_truth_degraded,
    )
    await _pub_bus.publish(
        settings.channel_phi_reward,
        PhiIntrinsicRewardEnvelope(source=_svc_ref(), correlation_id=uuid4(), payload=reward),
    )
    _PHI_PREV_PHI = out.phi
    _PHI_PREV_RECON = out.recon_error
```

Frozen path: skip encoder update and reward emit; keep cold-start/last-good headline.

- [ ] **Step 4: Run tests — expect PASS**

- [ ] **Step 5: Commit**

---

## Task 10: Manifest version mismatch + integration tests

**Files:**
- Extend: `tests/test_phi_encoder_mlp.py`
- Extend: `services/orion-spark-introspector/tests/test_phi_reward_emit.py`

- [ ] **Step 1: test_manifest_version_mismatch_refused**

```python
def test_manifest_version_mismatch_returns_none(tmp_path: Path) -> None:
    manifest = _tiny_manifest(["coherence"])
    manifest = manifest.model_copy(update={"features_version": "seed-v1"})
    (tmp_path / "manifest.json").write_text(manifest.model_dump_json())
    np.savez(tmp_path / "weights.npz", W1=np.zeros((1, 1)))
    assert PhiEncoderRuntime.load(tmp_path, expected_features_version="seed-v2") is None
```

- [ ] **Step 2: Run full Plan 2 pytest lane**

```bash
pytest tests/test_phi_encoder_schema.py tests/test_phi_encoder_mlp.py tests/test_phi_encoder_fit_script.py \
  tests/test_inner_state_trajectory_features.py tests/test_inner_state_features.py \
  services/orion-spark-introspector/tests/test_substrate_reads.py \
  services/orion-spark-introspector/tests/test_inner_state_emit.py \
  services/orion-spark-introspector/tests/test_phi_reward_emit.py \
  services/orion-substrate-runtime/tests/test_execution_trajectory_endpoint.py -q
```

- [ ] **Step 3: Commit**

---

## Task 11: Evals

**Files:**
- Create: `services/orion-spark-introspector/evals/eval_phi_encoder_fit.py`
- Create: `services/orion-spark-introspector/evals/eval_phi_reward_bounded.py`

- [ ] **Step 1: eval_phi_encoder_fit.py** — train on corpus slice (or synthetic), print recon_error p50/p95, probe summary; exit 0 on success.

- [ ] **Step 2: eval_phi_reward_bounded.py** — replay fixture ticks through encoder forward; assert `|delta_phi| < 0.5`, no NaN.

- [ ] **Step 3: Run evals**

```bash
python services/orion-spark-introspector/evals/eval_phi_reward_bounded.py
```

- [ ] **Step 4: Commit**

---

## Task 12: sql-writer durable phi_reward (minimal)

**Files:**
- Modify: `services/orion-sql-writer/` bus subscribe list (grep `channel_` / subscribe pattern)

- [ ] **Step 1: Grep existing inner_features consumer**

```bash
rg "inner_features|orion:self" services/orion-sql-writer services/orion-hub
```

- [ ] **Step 2: Wire subscribe + persist handler for `orion:self:phi_reward`** following the same pattern as `orion:spark:telemetry` (thin — store JSON payload).

- [ ] **Step 3: Gate test or smoke documenting durable path**

- [ ] **Step 4: Commit**

---

## Task 13: Agent gate + restart

- [ ] **Step 1: Run agent-check slice**

```bash
git diff --check
python scripts/sync_local_env_from_example.py
python scripts/check_env_template_parity.py
python scripts/check_schema_registry.py
python scripts/check_bus_channels.py
pytest tests/test_phi_encoder_schema.py tests/test_inner_state_trajectory_features.py \
  services/orion-spark-introspector/tests/test_phi_reward_emit.py -q
```

- [ ] **Step 2: Restart required**

```bash
docker compose --env-file .env --env-file services/orion-substrate-runtime/.env \
  -f services/orion-substrate-runtime/docker-compose.yml up -d --build
docker compose --env-file .env --env-file services/orion-spark-introspector/.env \
  -f services/orion-spark-introspector/docker-compose.yml up -d --build
```

Operator steps after corpus accrues:

```bash
python scripts/fit_phi_encoder.py --corpus /mnt/telemetry/phi/corpus/inner_state.jsonl --out /mnt/telemetry/models/phi/encoders/<version>
python scripts/fit_phi_encoder.py --eval-only --manifest /mnt/telemetry/models/phi/encoders/<version>
python scripts/fit_phi_encoder.py --promote --version <version> --encoders-root /mnt/telemetry/models/phi/encoders
# set ORION_PHI_ENCODER_ENABLED=true, restart spark-introspector
```

---

## Spec coverage self-review

| Spec requirement | Task |
|---|---|
| GET /projections/execution_trajectory | Task 1 |
| seed-v2 cognitive features + active run filter | Task 4 |
| Grammar-truth HTTP + fail closed | Task 3, 6 |
| MLP mlp_shallow_v1 inference | Task 7, 9 |
| Offline fit + promote gates | Task 8 |
| PhiEncoderManifestV1 + PhiIntrinsicRewardV1 | Task 2 |
| orion:self:phi_reward channel | Task 2 |
| Corpus telemetry path + migration | Task 5 |
| INNER_FEATURES_VERSION=seed-v2 | Task 5 |
| Probes + attributions | Task 7, 8 |
| Reward suppressed when frozen | Task 9 |
| sql-writer consumer | Task 12 |
| Step 3a recorder/metabolism | **Separate plan** |

---

## Non-goals (this plan)

- PhiValueRecorder, episode reducer, drive feed → `docs/superpowers/plans/2026-07-08-phi-intrinsic-reward-value-learning.md`
- Step 3b θ learning (`ValueUpdateV1`) → deferred spec
- In-process training
- Auto-promote without `--promote`
