# --- Global ---
PROJECT=orion-janus
NET=app-net

# --- Service Identity ---
SERVICE_NAME=meta-writer
SERVICE_VERSION=0.1.0
PORT=8210

# --- Orion Bus Configuration ---
ORION_BUS_ENABLED=true
ORION_BUS_URL=redis://${PROJECT}-bus-core:6379/0

# --- Bus Channels ---
# meta-writer listens to raw tag or collapse data and publishes enriched metadata

# incoming (listener)
CHANNEL_EVENTS_TAGGED=orion:tags:raw

# outgoing (publisher)
CHANNEL_EVENTS_ENRICHED=orion:tags:enriched

# optional core / triage integrations
CHANNEL_EVENTS_TRIAGE=orion:collapse:triage
CHANNEL_RDF_ENQUEUE=orion:rdf-collapse:enqueue

# --- Startup Behavior ---
STARTUP_DELAY=5
