# Consumer-first rollout shipped in PR #2211. Activation still requires the
# additive attempt migration and a controlled receipt/activity smoke.
enabled: true
policy_id: visual_baseline.v1
interval_sec: 5400
retry_sec: 600
freshness_sec: 60
timeout_sec: 2
target_id: host:circe_gpu
thought_url: http://orion-athena-thought:7155
