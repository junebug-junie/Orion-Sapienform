name: orion_analytics
version: 1.0.0
config-version: 2

profile: orion_analytics

flags:
  send_anonymous_usage_stats: false

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

models:
  orion_analytics:
    +materialized: view
